/**
 * "/" — the desk at rest. The visible scene is the persistent `<DeskStage>` in
 * the layout; this route just contributes the page heading.
 */
export default function Home() {
  return <h1 className="sr-only">Jayden Chen — developer &amp; hardware tinkerer</h1>;
}
